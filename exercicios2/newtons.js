const peso=Number(100);  
let newton=(10);
let calculo=0; 

if(peso=>calculo){ 
    calculo = peso*newton
    console.log('o peso de newtons é de ',calculo,' ou seja e muito pesado'); 
}else{ 
    console.log('caso haja um erro de calculo no '); 
    console.log(peso,'de',calculo);
} 
